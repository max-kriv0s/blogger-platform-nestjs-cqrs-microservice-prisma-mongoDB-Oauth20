export const ERROR_FORMAT_EMAIL =
  'The email must match the format example@example.com';

export const ERROR_LENGTH_USERNAME = `
{Minimum number of characters 6}
{Maximum number of characters 20}`;

export const ERROR_LENGTH_PASSWORD = `
{Minimum number of characters 6}
{Maximum number of characters 20}`;

export const ERROR_PASSWORDS_MUST_MATCH = 'Passwords must match';
export const ERROR_PASSWORD_MUST_CONTAIN = `Password must contain 0-9, a-z, A-Z, ! \" # $ % & \' ( ) * + , - . / : ; < = > ? @ [ \\ ] ^ _\` \{ \| \} ~`;

export const ERROR_EMAIL_IS_ALREADY_REGISTRED =
  'User with this email is already registered';

export const ERROR_USERNAME_IS_ALREADY_REGISTRED =
  'User with this username is already registered';

export const USER_NOT_FOUND = 'User not found';
